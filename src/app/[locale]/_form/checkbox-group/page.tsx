import React from 'react';
import CheckboxGroupClient from '@/app/[locale]/_form/checkbox-group/client';

const CheckboxGroupPage = () => {
	return <CheckboxGroupClient />;
};

export default CheckboxGroupPage;
